# @package _global_

# runs with execution time profiling

defaults:
  - default

steps:
  train:
    trainer:
      max_epochs: 1
      profiler: "simple"
      # profiler: "advanced"
      # profiler: "pytorch"
  test:
    trainer:
      max_epochs: 1
      profiler: "simple"
      # profiler: "advanced"
      # profiler: "pytorch"

